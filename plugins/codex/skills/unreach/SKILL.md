---
name: unreach
description: Find dead, unused, and orphan code via the Unreach MCP.
---

When the user asks about dead/unused/orphan code, call MCP `unreach.scan` then `unreach.plan`. Do not guess. Never delete files automatically.
